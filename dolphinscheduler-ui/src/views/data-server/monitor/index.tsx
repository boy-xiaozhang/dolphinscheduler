import {defineComponent} from "vue";

const Monitor = defineComponent({
    name: 'monitor',
    render() {
        return (<div>
            Monitor Index
        </div>)
    }
})

export default Monitor